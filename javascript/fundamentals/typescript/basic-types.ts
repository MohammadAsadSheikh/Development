// Number
let age: number = 25;

// String
let username: string = "Asad";

// Boolean
let isLoggedIn: boolean = true;

// Array
let numbers: number[] = [1, 2, 3];

// Object
let user: object = {
  name: "Asad",
  age: 25
};

// Any (avoid using frequently)
let data: any = "Hello";

data = 100;

// Unknown
let value: unknown = "TypeScript";

// Null
let emptyValue: null = null;

// Undefined
let notAssigned: undefined = undefined;

console.log(age);
console.log(username);
console.log(isLoggedIn);
console.log(numbers);
console.log(user);
console.log(data);
console.log(value);